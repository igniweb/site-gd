;(function (app, $) {

    'use strict';

    // Run modules
    app.module(
        'search',
        'datatables'
    );

    // Run application onReady
    //app.ready(function () {
    //});

})(App, jQuery);
